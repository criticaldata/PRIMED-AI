name: ecg_fm
repo_id: wanglab/ecg-fm
checkpoint: mimic_iv_ecg_physionet_pretrained.pt
checkpoint_path: /orcd/pool/006/lceli_shared/weights/mimic_iv_ecg_physionet_pretrained.pt
embed_dim: 768
seq_len: 64
sample_rate: 500
signal_length: 5000
n_leads: 12
frozen: true
pooling: mean
# Set use_stub: true for offline dev/CI without fairseq_signals + checkpoint.
use_stub: false
# ORCD HuBERT-ECG pooled embeddings (used by M06 probe today):
hubert_ecg_parquet: /orcd/pool/006/lceli_shared/ecg-hubert/mimic-iv-ecg-ve.parquet
